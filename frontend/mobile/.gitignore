# Build output
dist/

# Capacitor native projects — generated by `cap add`; keep them out of the repo by
# default (regenerate locally). Remove these lines if you choose to commit them.
android/
ios/

# Capacitor copies the web build here
.capacitor/

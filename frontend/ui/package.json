{
  "name": "@kodlyft/ui",
  "version": "0.1.0",
  "description": "KodLyft FSM shared Vue component library — themed once from design tokens, used across console, portal and mobile.",
  "private": true,
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": "./index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "typecheck": "vue-tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "peerDependencies": {
    "vue": "^3.5.0"
  },
  "dependencies": {
    "@kodlyft/design": "0.1.0"
  }
}
